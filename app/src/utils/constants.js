import {
  monthNames,
  dayNamesShort,
  monthNamesShort,
  dayNames,
} from "./localization";

export const notificationsCodes = {
  errorSC: "register-user-failed",
};

export const LOCALE_CONFIG = {
  monthNames,
  monthNamesShort,
  dayNames,
  dayNamesShort,
};
