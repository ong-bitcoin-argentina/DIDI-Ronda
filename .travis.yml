language: node_js
cache:
  npm: false
services:
  - mongodb
install:
  - npm i -g ganache-cli
  - npm i -g truffle
  - cd sc && npm ci && cd ..
before_script:
  - sleep 15
script:
  - cd sc
  - npm run test 